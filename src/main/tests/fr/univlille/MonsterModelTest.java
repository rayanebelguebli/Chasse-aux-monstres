package fr.univlille;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertFalse;
import static org.junit.Assert.assertNull;
import static org.junit.Assert.assertThrows;
import static org.junit.Assert.assertTrue;
import static org.junit.jupiter.api.Assertions.assertDoesNotThrow;

import org.junit.Test;
import org.junit.jupiter.api.BeforeEach;

import fr.univlille.models.GameModel;
import fr.univlille.models.MonsterModel;

public class MonsterModelTest {
    // @BeforeEach
    // public void init(){
    //     MonsterModel m1 = new MonsterModel(new GameModel(), new Coordinate(0, 0));
    // }

    // @Test
    // public void isMonsterMovementValidTest(){
    //     assertTrue(m1.isMonsterMovementValid(new Coordinate(1, 0), 1.0));
    //     assertFalse(m1.isMonsterMovementValid(new Coordinate(1, 1), 1.0));
    //     assertTrue(m1.isMonsterMovementValid(new Coordinate(0, 2), 2.0));
    //     assertFalse(m1.isMonsterMovementValid(new Coordinate(3, 3), 4.0));
    // }
}
